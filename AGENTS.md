# このプロジェクトの更新時の必須確認

2026-09-06のユーザー指示：
「これから、毎回更新時にレビューと画面操作による確認をお願いします。」

この指示は以後のアプリ更新にも適用する。画面操作による検証はユーザーから明示的に依頼されている。通常の更新確認について、その都度同じ依頼を求めない。ただし、実行環境の権限制約や承認要件は迂回しない。

## 更新の完了条件

1. 変更差分をレビューし、依頼どおりの挙動か、従来の仕様や保存データ、タイマー、通常版・コンパクト版の同期を壊していないか確認する。
2. `npm run verify` と `git diff --check` を実行する。
3. 自動テストとは別に、ブラウザーで実際の画面を表示し、クリック・タップ相当の操作、入力、スクロールなどで動作を確認する。コードの読み取り、HTTP応答の取得、DOMの模擬テストだけを画面操作確認の代わりにしない。利用可能なブラウザースキルの手順と権限制約に従う。
4. 変更した機能を一連の操作で確認し、件数の増減、時間ON/OFF、残り時間の補正、設定の開閉など主要操作も確認する。影響のある履歴編集、カード並べ替え、画面復帰、テーマ切替、通常版・コンパクト版の同期も検証する。
5. スマホ表示で、小カードの上下2段、主要な数字、下部固定ボタンの見切れ・重なりを確認する。320px、393px、430〜440px幅と、画面高・セーフエリアの影響を考慮する。ブラウザー上の再現確認と実機確認は区別する。
6. 問題があれば修正し、該当操作と影響範囲を再確認してから反映・完了報告する。報告では実施した確認と未確認の範囲を区別する。

画面操作はテスト用の状態・環境で行い、ユーザーの実際の稼働記録、履歴、設定を上書き・削除しない。画面確認が権限や環境の制限でできない場合は、その理由と未確認項目を報告し、必要な指示を求める。未実施の確認を成功扱いにしない。

既存の開発・保存互換・配信方針は `CLAUDE.md`、詳細な確認手順は `docs/MAINTENANCE.md` に従う。
