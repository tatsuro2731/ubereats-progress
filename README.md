# Uber配達進捗

Uber Eats 配達中に、目標件数に対して今の進捗が足りているかを確認するための小さな Web アプリです。

GitHub Pages で公開し、スマホのホーム画面に追加してアプリのように使う想定です。

## アプリ概要

- 目標件数、完了件数、残り稼働時間から、残り何件必要かを表示します。
- 12時間制限を基準に、目標達成までの余裕・遅れを確認できます。
- 任意で終了上限を設定すると、12時間制限と終了上限のうち短い方を有効残り時間として計算します。
- 残り時間カウントを ON にすると、稼働中の残り時間を自動で減らせます。
- 小カードの表示枚数と表示内容を変更できます。

## 基本の使い方

1. 目標件数を設定します。
2. 完了件数を現在の配達完了数に合わせます。
3. 残り稼働時間を設定します。
4. 必要なら終了上限を設定します。
5. 配達が完了したら `＋1件`、取り消す場合は `−1件` を押します。

## 入力項目の意味

### 目標件数

その日の目標配達件数です。

### 完了件数

すでに完了した配達件数です。

### 残り稼働時間

12時間制限のうち、あと何時間稼働できるかの目安です。

### 終了上限

「今日は何時までに終わりたいか」を入れる任意項目です。

- 終了上限なし: 12時間制限を基準に計算します。
- 終了上限あり: 12時間制限と終了上限のうち、短い方を有効残り時間として計算します。
- 現在時刻より前の時刻を入れた場合は、翌日の終了上限として扱います。例: 夜に `01:00` を設定すると `明日01:00上限` になります。

## 12時間制限との関係

このアプリは、12時間制限を固定の上限として扱います。

終了上限を設定していない場合は、従来通り12時間制限基準で表示します。

終了上限を設定した場合は、メイン表示・必要ペース・狙える件数・目標ペース・有効残り時間が終了上限を反映します。終了上限までに目標未達の見込みがある場合、終了予測には `上限超過` が表示されます。

## 残り時間カウント

残り時間カウントは、稼働時間を自動で減らすための補助機能です。

- 案件中・走行中は ON。
- 鳴り待ち・休憩中は OFF。
- ON中は残り稼働時間が自動で減ります。
- 手動で残り時間を変更すると、その値を基準に再計算します。

## ホーム画面に追加して使う

スマホのブラウザで GitHub Pages の公開URLを開き、iPhone なら Safari の共有メニューから「ホーム画面に追加」して使う想定です。

PWA表示、スマホ表示、小さめiPhone向けの表示調整を入れています。

## 注意点

- 表示される終了予測や必要ペースは、スマホの現在時刻を使った目安です。
- localStorage に入力値、終了上限、カード設定、残り時間カウント状態を保存します。
- 端末やブラウザの localStorage を削除すると、保存内容も消えます。
- リセットすると、完了件数、残り稼働時間、終了上限が初期状態に戻ります。

## 開発メモ

`manifest.webmanifest` では、同じ `apple-touch-icon.png` を `180x180` / `192x192` / `512x512` に指定しています。

今回の方針ではアイコン画像と manifest は変更しません。将来的には、サイズ別の PWA アイコン画像を用意するとより適切です。
